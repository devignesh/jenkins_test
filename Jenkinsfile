pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'testing purpose jenkins tool'
                sh '/home/cognier/projects/ansible_test/test.sh'
                // sh 'mkdir jenkins'
            }
        }
    }
}